# Project
This project uses TypeScript, React, and Tailwind.

## Installation
To install the dependencies, run the following command:
npm install

## Usage
To start the project, run the following command:
npm start